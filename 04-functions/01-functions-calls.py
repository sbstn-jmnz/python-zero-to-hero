# En la programación una función es un conjunto de sentencias con un nombre
# Cuando definimos una función, se especifica el nombre y las líneas de código que le pertenecen
# Luego para llamar a la función se usan los ()

print(print)

# Para ejecutar una función, sí o sí hay que terminarla con ()
print("Hola")
print(int(4.2))

from random import choice, randint

print(choice([1,2,3]))
print(randint(1,5))
